<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE flow PUBLIC  "liteflow" "liteflow.dtd">
<flow>

    <chain name="chain1">
        THEN(
         a, b,  WHEN(c, d)
        );
    </chain>

    <chain name="chain2">
        THEN(
        a, b, SWITCH(e).TO(c, d)
        );
    </chain>

</flow>